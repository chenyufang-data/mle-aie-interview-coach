# Extra dependencies for the Phase 0 STT experiment (grader/stt_eval.py) and
# the local audio fallback stack. Not needed by the app itself and not
# installed in the Docker image:
#   pip install -r requirements-stt.txt
elevenlabs>=2.65          # Scribe v2 batch + Realtime (WebSocket), Flash v2.5 TTS
websockets>=13            # used by the SDK's realtime client
faster-whisper>=1.2       # condition 6: local large-v3-turbo (CTranslate2); PyAV decodes webm/mp3
# GPU inference on Windows/Linux without a system CUDA install: the wheels
# carry the DLLs and stt_eval.py adds them to the DLL search path.
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*
