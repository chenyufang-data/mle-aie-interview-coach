# Backend (Python API) dependencies only. The frontend has none: public/ is
# dependency-free static JS, served by the backend or by nginx in Docker.
anthropic>=0.119.0
scikit-learn>=1.4

# Resume parsing (PDF); .docx is parsed with the standard library.
pypdf>=4.0

# Hybrid retrieval (BM25 + bge-small embeddings, retrieval_dense.py). Shipped
# 2026-09-04 after the pre-registered rule in docs/plan.md
# passed (docs/retrieval_evaluation.md): +13 points Recall@5 on paraphrased
# queries, no regression on curated ones. ONNX on CPU, no torch; the ~127 MB
# model downloads once into data/models/. If the import or download fails
# the server states why and serves BM25 alone.
fastembed==0.8.0
